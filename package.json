{
  "name": "restaurant-saas-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Restaurant SaaS Multi-tenant System with Org Admin, POS, Line-Mode KDS, Super Admin, and Express API",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm --prefix apps/api run dev",
    "dev:org": "npm --prefix apps/org-admin run dev",
    "dev:super": "npm --prefix apps/super-admin run dev",
    "db:setup": "npm --prefix apps/api run db:setup",
    "build:packages": "npm --prefix packages/shared-schemas run build && npm --prefix packages/ui run build",
    "build:api": "npm run build:packages && npm --prefix apps/api run build",
    "build": "npm run build:packages && npm run build --workspaces",
    "vercel-build": "npm run build:packages && npm --prefix apps/api run build"
  },
  "keywords": [
    "restaurant",
    "saas",
    "pos",
    "kds",
    "multi-tenant",
    "express",
    "react"
  ],
  "author": "",
  "license": "ISC"
}
